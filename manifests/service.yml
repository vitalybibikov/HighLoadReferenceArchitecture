apiVersion: v1
kind: Service
metadata:
    name: "365scorestest"
    labels:
        app: "365scorestest"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "365scorestest"
